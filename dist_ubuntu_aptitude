# vim:ts=4

Debian Aptitude手冊


1、 定義與介紹
==============================
aptitude 是 Debian GNU/Linux 系統中的的套件管理器，基於 apt 機制，整合了 dselect 和 apt-get 的所有功能，並提供的更多特性。特別是在依賴關系處理上。要使用 aptitude，只要打開您喜愛的終端，鍵入 aptitude 指令即可。

aptitude 的主視圖組織的非常有條理，上部視窗列出的是套件，根據類別和本機的狀態排序。

aptitude 主選單提供了七個選項：
	1. Updated Packages(升級的套件)
		列示已經升級到新版本的套件。通常當用戶完成選擇處理，aptitude 下載安裝了請求的套件後，這裏的套件都將被升級。

	2. New Packages（新套件）
		更新（aptitude update，或在 aptitude 中鍵入<u>）套件列表後所增加的套件，讓用戶了解 Debian 套件庫增加了哪些內容。
		每次更新，新的套件就會累加到這裏，直至鍵入<f>將累加清除，並將其彙整到主包庫中.

	3. Installed Packages(已安裝套件)
		列示已經安裝在機器上套件，且目前沒有更新的升級版本。

	4. Not Installed Packages(尚未安裝的套件)
		列示沒有安裝在機器上套件

	5. Obsolete or Locally Created Packages(廢棄或本地創建的套件)
		列示目前已安裝安裝在機器上套件，但是套件並非來自 /etc/apt/sources.list 。

	6. Virtual Packages(虛擬包)
		預先集合的套件合輯，例如，mail-transport-agent 就是由 postfix 和 sendmail，以及其它等等套件提供的

	7. Tasks(任務)
		任務是一組套件，以簡單的方法來選擇一組預定義的的套件。aptitude 允許對任務進行瀏覽和安裝。

aptitude 採樹狀結構組織，可使用方向鍵、<PageUp> 和 <PageDown> 進行定位。<Return> 或 <Enter> 用於展開或收起某個節點。

aptitude 的開視圖分為四列: 套件狀態和請求動作，套件名稱，當前安裝版本(或<none>)，最新版本.

aptitude套件列表中的套件狀態:
	v虛擬、B損壞、u解包、C預配置、H預安裝、c卸載未清除、p清除套件、i已經安裝、E內部錯誤

在aptitude套件列表中的請求操作:
	h保持、p清除、d刪除(卸載)、B損壞、i安裝、r重裝、u升級、

底部視窗給出主視窗當前顯示的上下文訊息，可以用<a>和<z>實現滾屏，<i>在不同訊息視圖間切換.最後，<D>則用於顯示和隱藏訊息區.

下邊是默認安裝時相關背景顏色說明:
	綠/安裝新套件、青/將套件升級的最新、品紅/刪除(或清除)套件、白/保存版本(禁止升級)




2、 快速開始
==============================
在 aptitude 的主視圖中鍵入<q>(quit)，即可退出 aptitude。鍵入<?>(help)，都可以喚出幫助訊息。

aptitude 是基於 apt 機制的，因此操作前需要配置 sources.list 檔案。並鍵入<u>(update)更新套件狀態資訊，相當於 apt-get update

如果有新的套件，會在 New Packages 選項下顯示。可鍵入<f>(forget that packages are new)，將其彙整到可用套件選項

移動游標到選項，然後鍵入<Enter>展開。選定一個套件後，有關的訊息會出現在屏幕底部。如要查看更詳細的訊息，鍵入<i>(information)，鍵入 <q> (quit)則會退出訊息顯示屏幕。

沒有安裝的套件(位於 New Packages 或 Not Installed Packages )，將其選定後，鍵入<+>(add package)，以標示安裝。

已經安裝的套件，可以執行保持，刪除，或清除等操作. 保持 <=>(hold) 是指保持現有版本，而禁止對其升級。刪除 <->(remove package) 標記已安裝套件為刪除狀態。 <_>(purge package) 命令則將套件標記為清除狀態。

完成標記後，可鍵入 <g>(go ) 執行作業。




3、 過濾與查詢
==============================
鍵入 </>，並提供一些判斷語句，來查詢套件的名稱。<\>用於查詢下一個符合查詢條件的資料。

同正則表達式一樣，可以用 ^ 和 $ 匹配名稱的起始字符和結束字符。 使用<l>，用戶可以以某種標准限制套件的顯示，這同樣適用於查詢。

下邊是一些重要的判斷謂詞:
條件			用途

~ahold		保持現有版本的套件
~b			損壞的套件
~d<text>	描述中含有<text>內容的套件
~g			無用的套件
~m<maint>	由<maint>維護的套件
~n<text>	名稱中含有<text>的套件
~V<version>	版本號中含有<version>的套件

這些謂詞是可以聯合起來使用，例如，~ahold~dmail 選擇在描述字中含有 mail 的被保持現有版本的套件
在兩個條件間使用 <|> 表示對兩個表達式為 或(OR) 的關係，~v|~b 選擇所有受損套件或虛擬套件
查詢不會忽略兩個謂詞間的空格！因此，~V.0 和 ~V .0 是不一樣的，後邊的一個不會有匹配結果
驚歎號表示相反的意義: !~b 查詢所有未受損的套件
可以使用括號來表達複雜的布爾邏輯:~g(~dmanual|~sdoc) 將查詢所有包含 manual 或在 doc 部分的受損套件
需要注意的是，當搜尋正則表達式中含有某些特殊字符，如 '()'，代字號(~)，和感歎號時，則必須使用代字號進行轉義.




4、 套件的選擇
==============================
套件的選擇可以通過瀏覽，然後按下對應鍵進行操作:

<+>	選定要安裝的套件

<->	選定要刪除的套件

<_>	選定要清除的套件

<=>	保持套件的當前版本，禁止升級

<:>	僅在 aptitude 會話期間鎖定套件

<L>	重裝套件

<M>	將套件標記為自動安裝。自動安裝的套件，在手動安裝的套件對其沒有依賴需求時會自動刪除。

<m>	將套件標記為手動

<R>	請求重新配置套件

<I>	立即安裝套件（以及依賴包）並暫時鎖定其它升級和安裝的套件。與apt-get install <package>相同。

<F>	禁止安裝某個版本的套件，但可用更高版本

<B>	reportbug，申報錯誤

<C>	下載並顯示套件的變更日志

<g>	進入所有請求變更的預覽視窗，如果在預覽屏鍵入此鍵，將執行變更操作。

<d>	查詢相關套件: suggest/recommanded/depends

<r>	查詢依賴包

</>	搜索

<\>	繼續搜索

<Ctl-u>	取消上一步的動作

展開一個套件的節點，會在屏幕上輸出套件的詳細訊息。在 aptitude 給出的解決方案不能滿需求時，可以在屏幕中方便的瀏覽相關聲明並交互的解決依賴問題。還可以使用 <Return> 展開節點，在主視窗用於操作套件選擇的按鍵也可在遵循相關聲明的情況下，應用於這裏列出的套件。鍵入<q> 返回上一級，關閉套件詳述視窗，返回套件列表




5、 受損的套件
==============================
當 aptitude 遇到不能解決依賴問題時，會以紅色背景高亮顯示有問題的套件，並顯示標題視窗中錯誤數量。

並會在標題視窗中顯示受損包的數量，例如: #Broken: 2

mygray Actions Undo Package Search Options Views Help 

mygrayf10: Menu ?: Help q: Quit u: Update g: Download/I 

mygrayaptitude 0.2.15.9 #Broken: 2 Will use 1118kB of 

mygray--- Packages being automatically installed to satisfy 

mygray--\ Packages being deleted due to unsatisfied depende 

mygrayid postfix -1974kB 2.1.5-9 

mygrayiB postfix-mysql 2.1.5-9

mygrayiB postfix-tls 2.1.5-9 

mygrayi webmin-postfix 1.180-4 

mygray--\ Packages to be installed 

mygraypi exim4 +32.8kB <none> 

mygray--- Packages which are suggested by other packages 

mygray 

受損的套件是指那些不能滿足依賴關系的，或相互沖突的套件(這種情況下兩個套件都是 broken 的)。 aptitude 禁止安裝 broken 的套件. 因此，需要首先修正這種問題:

當 broken 套件的個數非零，並且 aptitude 被告知動作未定時(用戶鍵入了 <g> 鍵)。在顯示這些未決問題摘要之前，aptitude 將試圖解決所有的問題。通常使用保守的方法進行自動修正，所以前面的所做的選擇有可能被丟棄。鍵入 <g> 前將一直檢查 broken 套件的數量，或者詳細顯示 broken 套件的訊息，這將丟棄前邊所做的修改。幸運的是，可以 <Ctl> 或 <Ctl-u> 來取消最後的操作，然後進行手動解決

當在 aptitude 中修複 broken 套件時，過濾（或限制）功能非常的有用。鍵入 <I> 進入 ~b，aptitude 將會只顯示 broken 的套件。使用套件細節列表，通過選擇缺少的依賴包，手動解決沖突，稍做努力就可修正出現的問題。另外，也可簡單的鍵入 <b>，找到第二個 broken 套件。

當 broken 的套件數量增加時，可能需要過濾顯示套件列表。鍵入 <I> 應用前面的過濾設置，以縮小仍處於 broken 狀態的套件列表(希望少了很多)。 重複操作，在短時間內就會修複所有 broken的 套件。




6、清潔系統
==============================
當 aptitude 選擇了套件以解決其它的依賴問題時，它將這個套件標記為自動安裝。因此，當依賴套件卸載時，這些套件也會自動卸載，這有助於系統保持清潔。也可以使用 <M> 和 <m> 手動修改安裝 標示: 首先添加這一標示，隨後就會卸載。 ~g 謂詞可以用於根據這一標示進行搜索和限制。

aptitude 這一功能非常的不錯， deborphan 也提供近似的功能，並且操作起來更加靈活.




7、 虛擬包
==============================
有時候，套件可能需要其它必需選擇的套件提供一個概念.這種需求的一個典型例子可以在套件的關聯訊息中找到.at被設計為依賴能發送電子郵件的程序. 在Debian中，有不下十種郵件傳輸代理套件，並不是寫死到at的依賴訊息中，套件只是簡單的通過依賴於概念包mail-transport- agent 來實現.提供了所需功能的套件都聲明提供了這種概念，在Debian中，是通過所謂的「虛擬包」來實現的.在系統中安裝了任意一個提供了mail- transport-agent虛擬包的套件，Debian套件工具都認為滿足了依賴關系.

mygrayv --\ mail-transport-agent <none> <none> 

mygray --- Packages which depend on mail-transport-agent 

mygray --\ Versions 

mygrayp xmail 1.21-3sarge1 

mygrayp masqmail 0.2.20-1sarge1 

mygrayp courier-mta 0.47-4sarge4 

mygrayp xmail 1.21-3 

mygrayp ssmtp 2.61-2 

mygrayp sendmail-bin 8.13.4-3 

mygrayi postfix 2.1.5-9 

mygrayp nullmailer 1.00RC7-22 

mygrayp masqmail 0.2.20-1 

mygrayp exim4-daemon-light 4.50-8 

mygrayp exim4-daemon-heavy 4.50-8 

mygrayp exim 3.36-16 

mygrayp esmtp-run 0.5.0-4 

mygrayp courier-mta 0.47-4

aptitude 的 Virtual Packages(虛擬包)選項可以用於快速定位提供同一功能的套件列表. 這樣當默認設置不能滿足您的需求時，可以使用這一功能快速的完成調整.




8、任務的概念
==============================
Debian 系統通常用於完成某些任務。比如，作為桌面系統、資料庫服務器、web 服務器、郵件服務器等等。 Debian 的任務的概念是指滿足某種需求的一系列典型的套件；上邊所指的任務，可安裝一組套件，來構建相應的服務器，或桌面環境

mygray--\ Tasks 

mygray --\ End-user 

mygray --\ Desktop environment 

mygrayp fortune-mod <none> 1:1.99.1-2 

mygrayp gdm <none> 2.6.0.8-1 

mygrayp gimp <none> 2.2.6-1 

mygrayp gnome <none> 64 

mygrayp gnome-desktop-environment <none> 64 

mygrayp kde <none> 5:44 

mygrayp kde-core <none> 5:44 

mygrayTasks are groups of packages which provide an easy way to select a 

mygraypredefinedset of packages for a particular purpose. 

mygrayIf you select a package，an explanation of its current state will appear 

mygrayin this space. 

傳統的程序 tasksel 提供了選擇這些套件集合的界面，這個功能被整合進 aptitude，並加以強化，因此 aptitude 更受歡迎

任務是抽象的虛擬套件，並沒有相關的真實 deb 檔案。另外，任務僅是建議，用戶可以不選擇建議中的一些套件。您可能會覺得更喜歡 aptitude 主選擇屏幕最下邊的任務界面.

與處理套件相似，任務可以視作單個的實體，或展開展示的建議的套件。可將任務展開，檢查一下建議安裝的套件列表。安裝任務所包含套件的一部分，而不是全部安裝

將任務描述文件放置到 /usr/share/tasksel 目錄下，來自定義任務。在 tasksel 套件安裝的 README 文件中提供了有關如何構建任務的文檔。




9、強大的命令行
==============================
aptitude 提供了命令行模式，可取代 apt-get 並具有 apt-cache 的查詢能力，並在 aptitude 的交互接口增加了搜索判斷

通常，aptitude的命令行請求形如:		# aptitude action <arguments...>

常用的一些操作:
aptitude update
aptitude upgrade
aptitude dist-upgrade
aptitude install pkgname
aptitude remove pkgname
aptitude purge pkgname
aptitude search string
aptitude show pkgname
aptitude clean
aptitude autoclean
aptitude install A+ B- C_ D= →	安裝A，刪除B，清除C，保持D。'+'是冗餘的，因為默認為安裝.




10、操作日志
==============================
aptitude 將作業經過寫入 /var/log/aptitude。這個檔案可用於跟蹤安裝和刪除套件。
如果使用 apt-get 安裝套件，用 dpkg 卸載套件，aptitude 的日志很快就會同步。
另外，aptitude 只記錄請求，如果某一動作失敗了，是不會記錄的。




